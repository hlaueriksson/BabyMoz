package specs.junit;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

@RunWith(JUnit4.class)
public class RunJUnitTests {

    @Test
    public void should_run_all_tests_in_this_module()
    {
        // All in package
        // In single module: IntelliJ IDEA will look for the test classes of the current package only in the module selected in the
    }
}
